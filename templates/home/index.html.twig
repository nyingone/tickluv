{% extends 'base.html.twig' %}

{% block title %}BookingOrderForm!{% endblock %}

{% block body %}
{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>#}
<script>

</script>
<div class = "row">
<div class= "col-md-12"> 
    <h1>{% trans %} Hello {% endtrans %} {{ controller_name }}! ✅</h1>
 
    <div class = "row">
        
        <div class= "col-md-6"> 
        {{ form_start(form) }}
        {# Render the BookingOrder fields #}
            <h3>{% trans %} Your orders {% endtrans %} </h3>   
            
            <ul class="bookingOrders" id="bookingOrderList" data-prototype="{{ form_widget(form.bookingOrders.vars.prototype)|e('html_attr') }}">
                {# iterate over each existing BookingOrder and render fields to enter #}
                {% for bookingOrder in form.bookingOrders %}
                    <li>
                        {{ form_row(bookingOrder.expectedDate)}}
                        {{ form_row(bookingOrder.partTimeCode)}}

                        <ul class="visitors" id="visitorList" data-prototype="{{ form_widget(bookingOrder.visitors.vars.prototype)|e('html_attr') }}"> 
                            <h3>{% trans %} Your guests {% endtrans %} </h3>  
                            {# iterate over each existing Visitor and render fields to enter #}   
                            {% for visitor in bookingOrder.visitors %}                                             
                                <li>
                                   {% include('inc/_newVisitor.html.twig') %}
                                </li>
                            {% endfor %} 
                        </ul>
                {% endfor %}  
            </ul>     
        </div>
    
        <div  class= "col-md-6">
            <h3>{% trans %} Your references and tickets order{% endtrans %} </h3> 
            {% include('inc/_newCustomer.html.twig') %}
        </div>
    </div>
        
    <input type="submit" class="btn btn-primary" value= "{% trans %} Go ahead {% endtrans %} " /> -->
                
    {{ form_end(form) }}
 
</div>
</div>
<script> </script>
{% endblock %}

